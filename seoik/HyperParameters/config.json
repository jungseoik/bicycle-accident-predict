
{
    "input_dim": "feature.shape[1]",
    "layers": [512,512,256, 3],
    "activations": ["relu","relu","relu", "softmax"],
    "dropout": 0.2,
    "lr": 0.001,
    "epochs": 20,
    "batch_size": 256,
    "criterion" : "nn.CrossEntropyLoss()",
    "optimizer" : "Adam"
}